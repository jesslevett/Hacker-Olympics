print("Welcome to our game. Today we will you have you guess a number that is randomly generated by the computer")
value = random.choice(range(1,100))
number = input("Enter a number between 1 and 100")
print(number)

while value != number:
  if number < value:
    print("Too low try again!")
